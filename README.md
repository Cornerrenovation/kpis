# Corner — Growth & Design KPIs (public, aggregate-only)

Auto-generated weekly from the private `cornerrenovation-core` repo. Contains
**aggregate figures only** — counts, conversion rates, average durations — and
**no client names or notes**. `noindex` + `robots.txt` keep it out of search.

Live: https://cornerrenovation.github.io/kpis/
